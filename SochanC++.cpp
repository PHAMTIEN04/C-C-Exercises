// In ra so chan 

#include <bits/stdc++.h>

using namespace std;
int main(){
	int a , b , c ; 
	cin >> a >> b >> c ;
	if (a % 2 == 0) 
	{
		cout << a << " so chan ";
	}
	if (b % 2 == 0 )
	{
		cout << b << " so chan ";
	}
	if (c % 2 == 0)
	{
		cout << c << " so chan ";
	}


	return 0;
}

